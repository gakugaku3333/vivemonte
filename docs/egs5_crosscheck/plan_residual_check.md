# 検証計画: Phase 2b系統差−1.71%のうち未説明の約36%（−0.6pt）の要因調査

作成日: 2026-07-17。[plan_compton_transfer_check.md](plan_compton_transfer_check.md)で
「コンプトンエネルギー移行分布の差」が観測−1.71%の約64%（Δ_pred=−1.095%）を説明する
部分的要因と確定した後に残った、**約−0.6pt の未説明残差**の原因究明計画。
前回同様、実行前に判定基準を事前登録する（vive-auditor所見を受け、判定基準確定時点で
本計画書を一度コミットして事前登録の時点証明とする）。

## 現状の残差の算定

- 観測（47ビン平均）: (EGS5−ChatCarlo)/ChatCarlo = **−1.71%**（範囲−0.71〜−3.45%）
- 説明済み（コンプトン移行分布差、60 keV一次成分の机上検算）: **−1.095%**
- 残差: **約−0.62pt**（(1−0.0171)/(1−0.01095)−1）

ただし−1.095%は**60 keV一次光子成分のみ**の値である点に注意。実測ビンは散乱で軟化した
スペクトルの混合であり（表面ビンでもBSF≈1.48相当の散乱寄与）、Δ_predのエネルギー依存を
畳み込んだ「本当の予測値」はまだ計算していない。つまり残差−0.6ptには
**(a) 同じコンプトン仮説のスペクトル畳み込み不足**と**(b) 真に独立な別要因**の両方が
混在しうる。本計画はまずこの2つを切り分ける。

## 候補の棚卸し

| # | 候補 | 予想符号 | 予想規模 | 検証コスト |
|---|---|---|---|---|
| 1 | コンプトン仮説のスペクトル畳み込み不足（Δ_pred(E)のエネルギー依存×局所スペクトル軟化） | 不明（下記注） | 最大±数×0.1pt | 低（机上、Step 0a） |
| 2 | ChatCarloのμen/ρ log-log補間バイアス（散乱光子が落ちる30〜60 keV帯、vive-auditor所見7の持ち越し） | 不明 | 不明（要上界評価） | 低（机上、Step 0b） |
| 3 | 密度差（PEGS5水テンプレート1.001 vs ChatCarlo 1.000） | 深部で負（EGS5側の減弱が0.1%強い） | 表面≈0、10 cm深で−0.2pt程度の深さ依存傾斜 | 低（解析伝播、Step 0c） |
| 4 | コンプトン断面積−0.070%・全断面積−0.035%の差の深さ方向伝播 | 深部で正（EGS5の減弱が弱い） | 10 cm深で+0.07pt程度 | 低（解析伝播、Step 0c） |
| 5 | 蛍光X線の非輸送（O K殻0.543 keV、蛍光収率0.83%） | — | <0.01pt（両コード共通近似） | 低（数値上界のみ） |
| 6 | EGS5のシード変更再現性（Phase 1/2aから持ち越し） | — | 統計変動の確認のみ | 中（再実行1回） |
| 7 | 上記で説明できない未知の要因（推定量の実装差など） | 負のはず | 残り全部 | 高（Step 2で分解） |

注（候補1の符号）: エネルギーが下がるとt_KN/t_Sqの乖離自体は拡大する（60 keV −2.40% →
20 keV −6.59%）が、同時に光電割合が急増して（7.8% → 75.4%）コンプトン成分のkerma寄与が
希釈される。正味のΔ_pred(E)の形は打ち消し合いの結果であり、手計算の当て推量ではなく
Step 0aで数値的に確定させる（記憶や概算で符号を決めない）。

候補2〜5は仮に全部が最悪方向に重なっても−0.6ptに届くかどうか、という規模感。
そこで本計画の中心は、個別候補の積み上げではなく**決定打となる1本の実験**に置く。

## 決定打: EGS5をINCOH=1で再実行する（Step 1）

Phase 1のIBOUND=0/1の教訓と同型のアプローチ。PEGS5入力を`IBOUND=1, INCOH=1`
（ICPROF=0のまま）に変更すると、EGS5のコンプトン抽選はS(q)重み付き棄却になり、
ChatCarlo/XAAMDIが依拠するS(q)補正込み散乱理論と**物理モデルが揃う**
（机上検算で t_Sq/t_XAAMDI = +0.05% の一致を確認済み）。

このときの60 keV一次成分の予測は既に計算済み（[check_compton_transfer.py](check_compton_transfer.py)）:

    Δ_pred(t_Sq) = +0.179%   （PEGS5光電断面積が+0.408%高いことによる+0.19%が主）

したがってINCOH=1再実行後の47ビン平均 Δ_avg1 の着地点が、残差の正体を直接語る:

- **Δ_avg1 ≈ +0.2%（畳み込み補正後の予測値）に着地** → 残り36%は「一次光子近似の
  誤差」でしかなく、コンプトン移行分布差＋光電断面積差で**全て説明済み**だったことになる。
- **Δ_avg1 ≈ −0.4〜−0.6%に着地**（観測−1.71%からコンプトン分だけ回復し、負の残差が
  同規模で残る）→ **独立な別要因が実在**する。Step 2の個別分解に進む。

2つの帰結は約0.6pt離れており、47ビン平均の統計精度（n=1e8で各ビン0.17〜0.89%、
平均では0.1pt未満）で明確に判別できる。

**注（2026-07-17、Step 0完了後に追記）**: 以下の中心予測+0.179%はStep 0の
スペクトル畳み込み実行前の単一エネルギー(60 keV一次成分のみ)近似であり、
後述「Step 0 実行結果」節で判定基準ごと更新済み（新しい中心値−0.81%）。
この節はStep 0以前の検討過程の記録として残す。

## 事前登録の判定基準

Step 0aの畳み込み計算で予測値を確定してから（かつStep 1実行前に）本計画書に追記し、
その時点で再コミットする。判定は47ビン平均 Δ_avg1 = (EGS5_INCOH1−ChatCarlo)/ChatCarlo:

- **全説明と判定**: Δ_avg1 が「Step 0a畳み込み予測(INCOH=1構成) ±0.3pt」以内、かつ
  |Δ_avg1| < 0.5%。→ 残差調査は完了。Phase 2bの−1.71%は「物理モデル構成差＋
  データソース微小差」として全容解明とする。
- **独立残差の実在と判定**: Δ_avg1 ≤ −0.5%（観測と同符号で有意に残る）。
  → Step 2の個別分解へ。
- **異常（仮説体系の再考）**: Δ_avg1 > +0.5%、または畳み込み予測から±0.3pt超の乖離、
  または正規化形状一致（Phase 2bで確認済み）が崩れる場合。
- **副基準**: シフト量 (Δ_avg1 − Δ_avg0) = (Δ_avg1 − (−1.71%)) が畳み込み予測シフト
  ±0.3pt以内であること。ビン別には元の事前登録基準（統計誤差1%未満のビンで2σかつ
  相対差2%以内）の合否も記録する（Phase 2b本体の判定を書き換えるためではなく、
  INCOH=1構成での参考値として）。

## Step 0: 机上検算（メインセッション、EGS5実行不要）

### 0a. スペクトル畳み込みΔ_pred（INCOH=0構成とINCOH=1構成の両方）

1. ChatCarlo側のPDD計算スクリプト（[run_chatcarlo_pdd60.py](run_chatcarlo_pdd60.py)の
   track-length推定量）を拡張し、代表ビン（表面z0-1、中深z5-6、深部z10-11、z14-15）の
   **track-lengthフルエンススペクトル**（5 keV刻みで十分）をタリーする。自前コードなので
   追加は容易。n=1e6程度で形は十分。
2. Δ_pred(E) = [σ_photo,PEGS5(E) + Σ_c,PEGS5(E)·t(E) − μen/ρ(E)] / (μen/ρ(E)) を
   t=t_KN(E)（INCOH=0構成）とt=t_Sq(E)（INCOH=1構成）の両方で、10〜60 keVの
   細かいグリッドで計算する（`check_compton_transfer.py`の関数を再利用。
   PEGS5断面積のエネルギー依存は`extract_pegs5_photo_xs.py`で任意エネルギー抽出可能）。
3. ビンごとに kerma重み付き平均 Δ_pred_conv(bin) = Σ φ(E)·μen(E)·E·Δ_pred(E) /
   Σ φ(E)·μen(E)·E を出し、47ビン平均相当の予測値を確定する。
4. 検算: INCOH=0構成のΔ_pred_convが観測のビン別パターン（−0.71〜−3.45%、深部ほど
   ばらつき）をどこまで再現するかを見る。ここで観測−1.71%との差が±0.2pt以内まで
   詰まるなら、Step 1を待たずに「残差は畳み込み不足」の心証が強まる（ただし判定は
   Step 1の実測で行う）。

### 0b. μen/ρ log-log補間バイアスの上界評価

XAAMDI格子点（30, 40, 50, 60, 80 keV…）の中間エネルギー（35, 45, 55 keV）で、
ChatCarloのlog-log補間値と、第一原理近似（xraylib光電 + Σ_c×t_Sq(E)、0aで計算済みの
量の再利用）を比較し、補間バイアスの上界を得る。散乱スペクトルの主帯域（30〜60 keV）で
バイアスが0.1pt未満なら候補2は事実上棄却。

### 0c. 微小系統差の解析伝播（密度・断面積差）

- 密度1.001 vs 1.000: 局所kermaは質量係数ベースで一次では相殺するが、深さ方向の
  減弱exp(−μz)には効く（Δμ/μ=+0.1% → 10 cm深で−0.2pt）。深さ依存の傾斜として
  ビン別観測パターンと突き合わせる。あわせて、EGS5側Gy換算の分母質量4.004 g
  （=4 cm³×1.001）と減弱側の密度の使い分けに不整合がないかを再確認する。
- コンプトン−0.070%・全断面積−0.035%: 同様にexp(−Δμ·z)で伝播（10 cm深で+0.07pt、
  符号は密度と逆）。

成果物: `check_residual_convolution.py`（新設）＋実行ログ＋本計画書への結果追記＋
判定基準確定コミット。

## Step 0 実行結果（2026-07-17、[check_residual_convolution.py](check_residual_convolution.py)）

### 0a. スペクトル畳み込み

代表4ビン（z0-1, z5-6, z10-11, z14-15）でChatCarlo自身の輸送からkerma重み付き
エネルギースペクトルをタリーし（n=1,000,000、10-60 keVを2 keV刻み、AP=10keV未満の
寄与は全ビンで0.000%——除外の影響なし）、Δ_pred(E)を畳み込んだ結果:

| ビン | Δ(t_KN, XAAMDI補間分母) | Δ(t_Sq, XAAMDI補間分母) | Δ(t_KN, 第一原理分母) |
|---|---|---|---|
| pdd_z0-1 | −1.767% | −0.779% | −0.749% |
| pdd_z5-6 | −1.793% | −0.826% | −0.764% |
| pdd_z10-11 | −1.763% | −0.822% | −0.749% |
| pdd_z14-15 | −1.741% | −0.820% | −0.734% |

「XAAMDI補間分母」列はChatCarloが実際に使う値（log-log補間込み、実際の比較に対応）、
「第一原理分母」列はxraylib photo+compton×t_Sq（補間を経ない連続近似、0bで格子点上の
精度0.02〜0.08%を確認済み）を分母にした、**コンプトン移行分布仮説単独**の寄与。

**重要な限界（vive-auditor所見1・2への対応）**: 上表の重み付けスペクトルは
**ChatCarlo自身の輸送で生成したもの**であり、EGS5が実際に生成する二次光子
スペクトルではない（自己参照的な近似）。またPDD中心軸4ビンはすべて深さ方向の
ビンで、側方（OCR）ビンのスペクトル特性を代表する保証がない、という指摘を
監査で受けた。そこで側方代表4ビン（表面近傍・10cm深×中心軸/視野端寄り
x=-4〜-3cm）でも同じ畳み込みを追加実施したところ:

| 区分 | Δ(t_Sq, XAAMDI補間分母)平均 |
|---|---|
| PDDビン（4本） | −0.812% |
| 側方ビン（4本） | −0.803% |

両者はほぼ一致し（差0.01pt未満）、PDD専用スペクトルで得た予測値が側方ビンにも
妥当に外挿できることを確認した。ただし「ChatCarlo自身のスペクトルで重み付け」
という自己参照性自体は解消されていない——EGS5自身が測定した実スペクトルとの
直接比較は今回未実施（Phase 1の一次透過率0.88%一致から、ChatCarloの散乱
スペクトル形状がEGS5と大きく乖離している可能性は低いと考えるが、定量的な
裏付けではない）。

**重大な発見**: Δ(t_KN, XAAMDI補間分母)（現状のIBOUND=1×INCOH=0構成の予測、実際に
比較される値）は4ビンとも−1.74〜−1.79%（平均−1.766%）で、**観測−1.71%とほぼ一致する**。
一方、コンプトン移行分布仮説単独（第一原理分母）は−0.73〜−0.76%（平均−0.749%）に
留まり、単一エネルギー(60 keV一次成分のみ)の机上検算(−1.095%)より**むしろ小さい**。
差分の大部分（約−1.0pt）は0bで発見した別要因（ChatCarlo側のμen/ρ補間アーティファクト）
に由来する。

### 0b. μen/ρ log-log補間バイアス

格子点そのもの（20/30/40/50/60/80 keV、補間なし）での検証: 「第一原理近似」
（xraylib photo+compton×t_Sq）は全格子点でXAAMDI表値と相対差0.02〜0.08%——
第一原理近似そのものは高精度であることを確認。

一方、**格子点の中間エネルギー**（35/45/55 keV、いずれもXAAMDI表の格子点30-40-50-60-80
の間）では:

| E | XAAMDI log-log補間 | 第一原理近似 | 相対差 |
|---|---|---|---|
| 35 keV | 0.101037 | 0.099445 | **+1.601%** |
| 45 keV | 0.053419 | 0.052459 | **+1.829%** |
| 55 keV | 0.036470 | 0.035925 | **+1.516%** |

格子点で0.1%未満・中間点で+1.5〜1.8%という差は、再構成手法のバグではなく
**log-log線形補間そのものの曲率誤差**である（μen/ρ(E)は70〜100 keV付近に極小を持つ
凸型の曲線で、30-40-50-60-80 keVという粗い格子を線形補間すると中間点で系統的に
高く出る）。`scripts/fetch_nist_xaamdi.py`の抽出は正規表現で表内の全行を拾っており、
このNIST公表の標準格子（1,1.5,2,3,4,5,6,8,10,15,20,30,40,50,60,80,100…keV）自体が
疎であることに起因する——ChatCarlo側の抽出ミスではない。

**これはEGS5比較とは独立の発見**（ただしvive-auditor所見3: 定量的に確認したのは
**水のみ**）: ChatCarloのkerma/線量タリーは、水について30〜80 keV帯の格子点
以外のエネルギーで実際に約1.5〜1.8%高く出る系統バイアスを持つ。監査で
`chatcarlo/data/nist_xaamdi/`内の他材質（bone/soft_tissue/air/muscle/lung/
adipose）CSVも同じ疎な標準格子（20,30,40,50,60,80,100 keV）を持ち定性的に
同型の凸曲線を示すことが確認されており、バイアスが生じる**方向性**は他材質にも
及ぶ可能性が高いが、**バイアスの大きさが水と同程度(1.5〜1.8%)かは未計算**。
散乱で連続的にエネルギーが分布する実際のシミュレーションでは常にこの影響を
受けうるため、本EGS5比較に限らずChatCarlo全体の線量精度に関わる可能性がある
（対処法の検討・他材質での定量評価は本計画のスコープ外、lessons_learned.mdへの
記録候補）。

### 0c. 密度・断面積差の解析伝播

60 keV一次成分について、Beer-Lambert減弱exp(−μz)への寄与を分離:

| 深さ | 断面積差由来 | 密度差由来 | 合計 |
|---|---|---|---|
| 0.5 cm | +0.004 %pt | −0.010 %pt | −0.007 %pt |
| 5.5 cm | +0.040 %pt | −0.113 %pt | −0.074 %pt |
| 10.5 cm | +0.076 %pt | −0.216 %pt | −0.140 %pt |
| 14.5 cm | +0.104 %pt | −0.298 %pt | −0.194 %pt |

最深部（14.5 cm）でも合計−0.19 %ptと、残差約−0.6ptの説明には遠く及ばない規模。
**候補3・4（密度差・断面積微小差）は棄却**（観測残差を説明する量ではない）。

### Step 0 総合評価と判定基準の更新

観測−1.71%は、(a)コンプトン移行分布差（第一原理分母で−0.75%程度）と
(b)ChatCarlo側μen/ρ補間アーティファクト（中間点で+1.5〜1.8%、kermaを実際より
高く見せる→EGS5比較では相対的にEGS5を低く見せる）の**2つの独立な効果の合算**で
ほぼ説明できる、という新しい描像が浮かび上がった。密度・断面積微小差(0c)は無視できる。

**Step 1（INCOH=1再実行）の判定基準を更新する**（Step 0完了後・Step 1実行前の
時点でコミットし事前登録とする）。INCOH=1にするとEGS5のコンプトン抽選がS(q)重み付き
になり「コンプトン移行分布差」は消えるが、「ChatCarlo側の補間アーティファクト」は
ChatCarloの実装に起因するため**INCOH=1後も残るはず**。したがって元の登録基準
（Δ_avg1≈+0.2%で「全説明」）は**Step 0の発見前の見立てであり、そのまま使わない**。
更新後の中心予測値はΔ(t_Sq, XAAMDI補間分母)の畳み込み平均**−0.81%**
（4ビン平均: (−0.779−0.826−0.822−0.820)/4）に置き換える。

**更新後の事前登録基準**（Step 1実行前に確定）:

- **仮説体系を支持**: 47ビン平均Δ_avg1が−0.81%±0.3pt（−0.51%〜−1.11%）に着地。
  → 「コンプトン移行分布差＋μen/ρ補間アーティファクト」の2要因で−1.71%の残差が
  ほぼ全て説明されたと判定。
- **未知の第3要因が別途存在**: Δ_avg1が上記範囲外（特に−1.11%を超えてさらに負、
  または+0.5%を超えて正）。→ Step 2の個別分解へ進む。
- **異常**: 正規化形状一致が崩れる、またはΔ_avg1の符号がビンごとに反転するなど。

## Step 1: EGS5 INCOH=1再実行（egs5-operator）

- PEGS5入力のみ変更: `INCOH=0 → 1`（IBOUND=1のまま、ICPROF=0のまま。
  `egs5_hatch.f` 478–489行の整合性チェックはIBOUND=1が前提条件なので通る）。
- ユーザーコード`pdd60_phantom.f`は**一切変更しない**（47ビンタリーをそのまま流用）。
  ただしINCOH=1をhatchに伝えるために`incohr`の設定が必要かをegs5-operatorが
  ドキュメント・ソースで確認してから実行する（`egs5_block_set.f`のデフォルトは
  `incohr(i)=0`——PEGS5データを作るだけでなくリージョンフラグ`incohr(2)=1`の
  セットが必要なはず。Step 1の裏取りで確認済みの構造）。
- n=1e8（v1と同じ）、新規サブディレクトリ`pdd60_phantom_incoh1/`。既存成果物は変更しない。
- ChatCarlo側は再実行不要（既存`chatcarlo_pdd60_results.json`と比較）。
- 47ビンの相対差・σ・正規化形状比較をv1と同じ手順で算出し、事前登録基準に照合する。

## Step 2（条件付き）: 独立残差が実在した場合の個別分解

Step 1で「独立残差の実在」と判定された場合のみ。Step 0b/0cの上界評価で残った候補を
効果量の大きい順に潰す:

1. 密度を揃えた追加ラン（PEGS5水をρ=1.000で再生成）またはStep 0cの解析伝播値での補正。
2. μen/ρ補間バイアスが上界評価で無視できなかった場合、ChatCarloのkerma推定量を
   細分格子データで再評価。
3. シード変更再現性（EGS5をseedだけ変えてn=1e8×2、47ビン平均の再現幅を実測）。
4. それでも残る場合は推定量の実装差（ChatCarloのtrack-length×μen/ρ vs EGS5の
   衝突点edep集計）の境界条件・体積端効果を疑い、単純化ジオメトリ（1ビンだけの
   薄板）で最小再現を試みる。

## Step 1 実行結果（2026-07-17、egs5-operator、`docs/egs5_crosscheck/pdd60_phantom_incoh1/`）

### 実施内容

PEGS5入力`INCOH=0→1`（IBOUND=1のまま、ICPROF=0のまま）に変更。実行前の裏取り
（`$EGS5/tutorcodes/tutor7/`——EGS5公式のINCOH=1使用例）で以下を確認済み:

- `INCOH=1`自体はPEGS5への追加入力なしで有効（`ICPROF`/`IMPACT`とは異なり追加
  データ不要、tutor7.inpで確認）。
- ただしPEGS5データをINCOH=1で作るだけでは不十分——ユーザーコード側で
  リージョンフラグ`incohr(2)=1`（ファントムのリージョン番号）を明示的にセット
  する必要がある（`egs5_hatch.f` 466-470行の整合性チェックは「PEGS5ファイルの
  incoh==1かつibound==1」のみを要求し、`incohr`自体は強制しない）。tutor7.f
  132行目`incohr(2)=1`を実例として確認し、`pdd60_phantom_incoh1.f`に追記
  （47ビンタリーロジック自体は無改変、diffで確認済み）。この設定がなければ
  PEGS5データにS(q)テーブルがあっても黙って自由電子KNにフォールバックする
  ところだった——見落としがちな落とし穴を実行前に潰せた。
- 実行ログで`incohr(2)= 1 iprofr(2)= 0`が確認出力され、フラグが実際に有効に
  なったことを確認。コンパイルエラー・想定外の挙動なし。n=1e8、実行時間1分42秒
  （v1の1分38秒とほぼ同じ——S(q)棄却サンプリングによる速度低下は無視できる）。

### 結果

47ビン全数比較（`docs/egs5_crosscheck/pdd60_phantom_incoh1/comparison_table.csv`、
メインセッションが独立に再集計し一致確認済み）:

| 区分 | 平均相対差 (EGS5_INCOH1−ChatCarlo)/ChatCarlo |
|---|---|
| 47ビン全体 | **−0.633%** |
| PDD中心軸（15ビン） | −1.015% |
| 側方プロファイル（32ビン） | −0.454% |
| 統計誤差1%未満の43ビン | −0.654% |

参考: v1（INCOH=0）の47ビン平均は−1.71%。シフト量 = −0.633−(−1.71) = **+1.077pt**。

事前登録基準の43ビンでの2σ&2%基準は26/43合格（v1は6/43合格——不合格37・
対象外4、PDD_RESULTS.md「結果」節参照——絶対値の一致度が大きく改善）。

### 判定基準との照合

- 予測中心値: **−0.81%**（許容±0.3pt、範囲−0.51%〜−1.11%）
- 観測: **−0.633%** → **範囲内**
- 副基準（シフト量）: 予測+0.90pt vs 観測+1.077pt、差0.177pt → **許容範囲内**
- 正規化形状比較の崩れ・符号反転: なし（`comparison_table.csv`のreldiff列を
  目視確認した限り、PDD/側方の質的パターンはv1と同様、深いビンほど負側に
  大きい傾向が維持。**vive-auditor所見5**: これはv1本体で行った厳密な
  自己正規化手法——各コード自身の基準ボクセルで100%とし%pt差・σを算出
  （PDD_RESULTS.md「正規化による定量確認」節）——を再実施したものではなく、
  reldiff表からの定性的な観察に留まる。同等の厳密さでの再確認は今回未実施）

**判定: 「仮説体系を支持」**。事前登録した通り、「コンプトン移行分布差
（INCOH=0→1で解消）＋ChatCarlo側μen/ρ補間アーティファクト（INCOH設定によらず
残存）」の2要因で観測−1.71%のほぼ全量が説明されると判定する。

## Step 3: 監査・文書反映・コミット

1. vive-auditorにStep 0の計算・Step 1の数値・判定を独立検証させる。
2. `PDD_RESULTS.md`更新: 「原因の切り分け」節に残差調査の結果を追記し、
   ステータス行の「残り約36%は依然未解明」を確定結果で置き換える。
3. `docs/lessons_learned.md`に一般化できる教訓があれば追記（候補: 「一次成分のみの
   机上検算と、散乱スペクトル畳み込み後の予測は分けて扱う」）。
4. 成果物一式（スクリプト・実行ログ・EGS5新ディレクトリの入出力）をコミット。

## 落とし穴・限界（事前に認識しておくもの）

- **INCOH=1は角度分布も変える**: S(q)棄却はエネルギーと角度を同時に変えるため、
  kerma per collisionだけでなく透過・ビルドアップもわずかに変わる。予測との照合は
  47ビン平均で行い、ビン別の深さ依存の変化はStep 0aの畳み込みと合わせて解釈する。
  正規化形状比較も再実施する。
- **XAAMDIはWaller-Hartree S(q)ベース（Doppler広がりなし）**: ICPROF=0のまま
  にするのはこれに合わせるためで、ICPROF=1にすると逆にXAAMDIと物理がずれる。
- **ChatCarlo側を固定したままの比較**: v1（INCOH=0）とINCOH=1の2本のEGS5ランは
  どちらも同じChatCarlo結果と比較するため、2つのΔ_avgの差にはChatCarlo側の
  統計誤差が共通に効く（相関する）。シフト量の統計誤差はEGS5側2ラン分のみで評価する。
- **Δ_pred(t_Sq)=+0.179%は60 keV一次成分の値**: 判定基準の中心値はStep 0aの
  畳み込み後の値で置き換える（本計画書に追記してから実行する）。
- **PEGS5のINCOH=1テーブル生成**: PEGS5側でS(q)データの生成が必要
  （pegs5入力のINCOH行）。egs5-operatorがKEK配布のサンプル入力・マニュアルで
  書式を確認してから実行する（記憶で書かない）。
