# 抄録ドラフト: 第21回九州放射線医療技術学術大会（沖縄, 2026-11-14/15）

- 抄録締切: **2026-07-17(金) 12:00**（延長なし）
- 提出方法: UMIN演題登録システムのみ（オンライン新規登録）
- フォーマット制約: 【目的】【方法】【結果】【結論】の順、40字×10行（400字以内）、
  図表・複数行数式・特殊フォント不可
- 参加資格: 九州地域放射線技師会 or 日本放射線技術学会九州支部の会員・学生
  （非会員は非会員参加登録費8,000円で発表可）
- 倫理審査必須（「研究発表を行う時の倫理問題について」「利益相反の公開」への確認チェックが必要）

## 現在のドラフト（371字/400字, 2026-07-15更新 — EGS5相互検証の結果を反映）

**演題名**: 対話型AIで行えるモンテカルロ計算システムの開発

```
【目的】モンテカルロ法は放射線分野で有用だが、専門的なプログラミング知識が必要で参入障壁が高い。誰もが会話だけでシミュレーションを行える環境の実現を目指し、システムを開発した。
【方法】開発者はソースコードを記述せず、対話型生成AI(Claude Code)との自然言語の対話のみで、診断X線領域の光子輸送モンテカルロコードと、それを会話で操作するワークフローを構築した。
【結果】「胸部撮影室の散乱線分布を知りたい」と話しかけると、AIが撮影条件を聞き取り、体幹ファントムと撮影室を配置し、3D画像で配置を確認後に計算を実行、線量分布を提示する一連の流れを会話のみで行えた。
【結論】対話型AIにより、プログラミング知識がなくてもモンテカルロ計算システムを自作し会話で運用できることが示された。確立コードEGS5との比較でも一次透過率が相対1%以内で一致し、物理的妥当性を確認した。
```

（詳細な計算結果は[egs5_crosscheck/RESULTS.md](egs5_crosscheck/RESULTS.md)を参照）

## 経緯（方向性の絞り込み）

ユーザーの初期アイデア（「モンテカルロ法の民主化」「対話でシミュレーションコードを作れる」）
を軸に、対話しながら5案を検討した:

1. 民主化を前面に出す（参入障壁の除去）
2. 開発速度の実証（3日・約2,600行+テスト1,500行という定量事実で語る）
3. 検証方法論を主役にする（教訓集にある「AIの記憶由来の係数が最大17%ずれた」
   「桁バグが混入したが検証で捕まえた」という話を中心に据える）
4. 運用ワークフロー（コード開発ではなく、会話でシミュレーションを"使う"体験）
5. 民主化+検証の折衷

**採用: 案1（目的・方法の骨格）＋ 案4（結果の具体例）の統合**。理由はユーザー指定
（「この方法で以下のものを作った」という組み合わせ方の指示）。

### 途中で修正した構造上の問題

初期ドラフトの【結果】は「一次線透過率が解析解と一致し、物理的妥当性を確認した」と
書いていたが、これはユーザーから
**「コード開発とシミュレーション行為が混ざっている」**と指摘された。

この抄録が検証しているのは「対話だけでコード開発できるか」（開発プロセスの問い）で
あって、「このツールの計算精度が正しいか」（物理的妥当性の問い、PHITS比較の領分）
ではない。両者を混同すると、読み手にとって何を主張している研究なのかが曖昧になる。

**対応**: 【結果】に含む物理関連の記述は、常に「対話だけでここまで構築できた」という
**開発プロセスの成果**として書き、「シミュレーション結果が正しい」という**独立した
物理的主張**にはしない。今回の最終稿では、Beer-Lambert/NIST照合のくだり自体を削り、
案4の会話ワークフロー実例（ヒアリング→配置→3D確認→計算→提示）に差し替えることで
この混同を解消した。

## 今後の作業（次セッション）

1. **相互検証は実施済み・【結論】反映済み（2026-07-15、EGS5に切り替えて達成）**。
   PHITSは国内でもライセンス申請から配布まで約3週間かかり締切に間に合わないため、
   即日入手可能な確立コードEGS5（KEK配布）とのPhase 1相当比較に切り替えた。60 keV
   鉛筆ビーム＋水スラブ10cmの一次透過率で、束縛コンプトン断面積（IBOUND=1）に
   物理モデルを揃えた条件でviveMonte 12.589% vs EGS5 12.70%（相対0.88%、約1.7σ）と、
   計画書の事前基準「2σかつ2%以内」に適合。詳細は
   [egs5_crosscheck/RESULTS.md](egs5_crosscheck/RESULTS.md)。初回（自由電子KN,
   IBOUND=0）は相対4.7%の差で独立監査（vive-auditor）が差し戻し、真因が断面積の
   物理モデル差（束縛補正の有無）と判明したため再実行して基準適合に至った経緯あり。
   再監査サブエージェントはAPIセッション上限で中断したため、メインエージェント自身が
   KN解析式の妥当性・ログ転記・算術を独立再計算して確認した（サブエージェントによる
   完全な再監査ではない点を申し送り）。
   PHITS比較は11月の本発表までの追加データとし、ライセンス申請は別途進める。
2. UMIN登録フォームで別途必要な項目（本ドラフトには含まれない）:
   - 発表者・共同研究発表者の所属機関名・氏名・住所
   - 日本放射線技術学会または技師会の会員番号（未入会で番号不明なら99999）
   - 会費納入状況、連絡先
   - 「研究発表を行う時の倫理問題について」「利益相反の公開」確認チェック
3. 文字数には24字の余裕（376/400）があるため、微調整の余地あり。
